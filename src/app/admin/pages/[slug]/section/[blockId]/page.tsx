import { redirect } from "next/navigation";

/**
 * Content-type block editor removed — pages use module editors instead.
 */
export default function AdminPageSectionEditorPage() {
  redirect("/admin");
}
